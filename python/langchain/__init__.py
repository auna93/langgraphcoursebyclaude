# Alias `langchain` (superficie AVANZADA, C-RUNNER §tabla del shim, S12).
#
# El grounding importa mensajes/tools desde `langchain.messages` / `langchain.tools`
# (y sus alias `langchain_core.*`). Este paquete es un envoltorio fino sobre el shim
# real (`langgraph.graph.message`), no una reimplementación distinta.
